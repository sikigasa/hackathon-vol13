import 'package:flutter/material.dart';

class AddTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Tab'),
      ),
      body: const Center(
        child: Text('This is the Add Tab Page'),
      ),
    );
  }
}
